apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: test-selfsigned-cert
spec:
  dnsNames:
    - doc-index-updater.non-prod.mhra.gov.uk
    - medicines-api.non-prod.mhra.gov.uk
  secretName: test-selfsigned-cert
  issuerRef:
    name: test-selfsigned
