apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: argocd
spec:
  hosts:
    - argocd-mhra.red-badger.dev
  gateways:
    - istio-ingressgateway
    - istio-ingressgateway-internal
